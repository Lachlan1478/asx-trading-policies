[project]
name = "asx-trading-policies"
version = "0.1.0"
description = "Scrape ASX securities trading policies and extract collar-relevant terms"
requires-python = ">=3.12"
dependencies = ["anthropic>=1.0", "pydantic>=2", "pymupdf>=1.24", "requests>=2.31"]
